// import React, { Component } from "react";
// import ReactDOM from "react-dom";
// import * as Utils from "./utils";
// import { default as Sweetbot } from "./ui/Sweetbot";

// const create = props => {
//   const targetSelector = props.botSelector;
//   const target = targetSelector ? document.querySelector(targetSelector) : null;

//   !targetSelector
//     ? err("Bot selector", "was not provided")
//     : !target
//       ? err("Bot container", "could not be found in document")
//       : ReactDOM.render(<Sweetbot customprops={props} />, target);
// };

// export default create;
